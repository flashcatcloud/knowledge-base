---
title: "告警引擎（Monitors）常见问题"
date: "2025-11-07T20:56:02.070+08:00"
url: "https://docs.flashcat.cloud/zh/flashduty/monitors/faq"
---

#### 告警规则的执行不符合预期，如何调试？

告警规则的列表页面，有一个调试日志开关，您可以打开这个开关，之后告警引擎进程（monitedge）会输出该规则的详细执行日志，方便您排查问题。

monitedge 会把日志输出到标准输出，即 stdout。

- 如果是通过 Docker 方式运行的 monitedge，您可以通过 `docker logs <container_id>` 命令查看日志内容
- 如果是通过 Kubernetes 方式运行的 monitedge，可以通过 `kubectl logs <pod_name>` 命令查看日志内容
- 如果是通过 systemd 方式启动的 monitedge（即 Linux 安装模式），可以通过 `journalctl -u monitedge.service -f` 命令查看日志内容，或者直接查看 `/var/log/messages` 文件。monitedge 遵照云原生最佳实践的建议，把日志输出到标准输出，不会把日志写到单独的日志文件中，以方便日志收集系统采集，也方便轮转和压缩。
